int x;
x := input();

floata pot;
pot := input();

boolean mult;
mult := 0; 

for i:=0,i<pot,i++ {
    x := x * mult;
};

if x > -100a {
    output("Numero maior que 100");
} else {

    output("Numero menor que 100");
};

output("Valor :");
output(x);

output;